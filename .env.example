# Root example env (instructions)

# Server env variables (copy to server/.env)
MONGO_URI=
JWT_SECRET=
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme
PORT=5000

# Client env variables (copy to client/.env)
# VITE_API_URL=http://localhost:5000
